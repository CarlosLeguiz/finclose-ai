finclose_ai:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DBT_DUCKDB_PATH', '../data/finclose.duckdb') }}"
      threads: 4
      schema: main
    ci:
      type: duckdb
      path: ":memory:"
      threads: 1
      schema: main